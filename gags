# ===== A partir daqui, valor / cnpj_cpf / conta_pp / conta_ob já estão confirmados =====


    # Cada aba tem seu próprio DOM: locator() em aba_sigef nunca "vaza" pra aba_sei,
    # mesmo que os seletores sejam iguais nos dois sistemas.

    # Extrai algo do SIGEF
    #aba_sigef.bring_to_front()  # opcional, só pra você acompanhar visualmente
    # valor = aba_sigef.locator("#algumCampo").inner_text()

    # Usa esse dado no SEI
    #aba_sei.bring_to_front()
    # O SEI costuma usar iframes (árvore de processos, visualização do documento etc.),
    # então, se o locator direto não achar o elemento, procure dentro do frame:
    # aba_sei.frame_locator("#ifrVisualizacao").locator("#outroCampo").fill(valor)

    # Volta para o SIGEF para continuar o fluxo
    #aba_sigef.bring_to_front()
    # aba_sigef.click("#botaoConfirmar")

    # Repita esse padrão quantas vezes precisar — não há limite de quantas
    # vezes você pode alternar entre as duas abas dentro da mesma automação.